{
  "name": "workouts",
  "version": "1.0.0",
  "private": true,
  "description": "Workout logging — logs datés depuis un template, sans écrasement",
  "type": "module",
  "scripts": {
    "workout:new": "node scripts/workout-new.mjs",
    "site:build": "python3 scripts/build-site.py"
  }
}
